<resources>
    <!-- Base application theme. -->
    <style name="Theme.IELTS_PRACTICE_APP" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="recycler_horizontal_style">
        <item name="android:scrollbarSize">4dp</item>
        <item name="android:scrollbars">horizontal</item>
        <item name="android:scrollbarThumbHorizontal">@drawable/recycler_horizontal_scrollbar</item>
        <item name="android:scrollbarTrackHorizontal">@drawable/recycler_horizontal_scrollbar_bg
        </item>
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/top_corners_rounded</item>
    </style>
</resources>